rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /studybuddyUsers/{uid} {
      allow create, read, update, delete: if request.auth != null && request.auth.uid == uid;
    }
  }
}
